// HuffmanTree.cpp : 此文件包含 "main" 函数。程序执行将在此处开始并结束。
//

#include "pch.h"
#include "globle.h"
#include <iostream>
#include <algorithm>


//初始化哈弗曼树
void initHaffTree() {
	HaffTree[] = 
};


//使用weight关键字排序
bool compare(HaffNode a, HaffNode b) {
	return a.weight > b.weight;	//降序
}


//HaffTree中的元素进行排序
	//历遍数组

bool sort(HaffTree, HaffTree + 255, compare) {
	
};


int main()
{
}